<UserControl x:Class="Chat_App.UserControls.MessageChat"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Name="messageChat">

    <Border CornerRadius="0 10 10 10" Background="{Binding Path=Color,ElementName=messageChat}" Padding="10 7" HorizontalAlignment="Left" Margin="40 5 0 0">
        <TextBlock Text="{Binding Path=Message,ElementName=messageChat}" FontSize="14" Foreground="#ffffff" VerticalAlignment="Center" Margin="3 0 3 2"/>
    </Border>
</UserControl>